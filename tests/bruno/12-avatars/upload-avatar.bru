meta {
  name: Upload Avatar
  type: http
  seq: 3
}

put {
  url: {{baseUrl}}/api/avatars/YWRtaW5AZmVpcmIubG9jYWw
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:multipart-form {
  file: @file(12-avatars/avatar-test.png)
}

tests {
  test("should return 204", function() {
    expect(res.status).to.equal(204);
  });
}
